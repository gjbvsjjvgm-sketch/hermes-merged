# Hermes Web UI -- local machine config template
# Copy this to .env and fill in your values.
# start.sh sources .env automatically if present.
# All values are optional -- auto-discovery will fill in anything left blank.

# Path to your agent checkout (the repo that contains run_agent.py)
# YM_WEBUI_AGENT_DIR=/path/to/agent

# Python executable to use (defaults to the agent venv if found)
# YM_WEBUI_PYTHON=/path/to/python

# Bind address (default: 127.0.0.1 -- loopback only, safe default)
# YM_WEBUI_HOST=127.0.0.1

# Port to listen on (default: 8788)
# YM_WEBUI_PORT=8788

# Where to store sessions, workspaces, and other state (default: ~/.yusuf-mussa/webui-mvp)
# YM_WEBUI_STATE_DIR=~/.yusuf-mussa/webui

# Default workspace directory shown on first launch
# YM_WEBUI_DEFAULT_WORKSPACE=~/workspace

# Base directory for all Hermes state (affects all paths above if set)
# YM_HOME=~/.yusuf-mussa

# Path to your Hermes config.yaml (for toolsets and model config)
# YM_CONFIG_PATH=~/.yusuf-mussa/config.yaml

# Display name for the assistant in the UI (default: Yusuf Mussa)
# HERMES_WEBUI_BOT_NAME=Yusuf Mussa

# ── Paymob Payment Gateway ──
# Set to "true" for test/sandbox mode, "false" for live mode
# PAYMOB_TEST_MODE=true
# API key for test mode
# PAYMOB_TEST_API_KEY=
# API key for live mode
# PAYMOB_LIVE_API_KEY=
# Card integration ID for test mode
# PAYMOB_TEST_CARD_INTEGRATION_ID=
# Card integration ID for live mode
# PAYMOB_LIVE_CARD_INTEGRATION_ID=
# Mobile wallet integration ID for test mode
# PAYMOB_TEST_WALLET_INTEGRATION_ID=
# Mobile wallet integration ID for live mode
# PAYMOB_LIVE_WALLET_INTEGRATION_ID=
# HMAC secret for webhook verification (from Paymob dashboard)
# PAYMOB_HMAC_SECRET=
# Iframe ID for hosted payment page
# PAYMOB_IFRAME_ID=
